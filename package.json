{
  "name": "examples",
  "private": true,
  "description": "A turborepo of small static sites. Each src/<name>/ builds itself; dist/ is assembled from those builds at deploy time and is not versioned.",
  "packageManager": "pnpm@10.33.4",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter home dev",
    "build": "turbo run build $(node bin/examples.mjs --filter) && ./sync.sh",
    "build:home": "./build-home.sh",
    "preview": "pnpm --filter home preview",
    "shots": "./preview.sh",
    "og": "node .github/og.mjs"
  },
  "devDependencies": {
    "@examples/dev": "workspace:*",
    "turbo": "^2.10.9"
  }
}
