# Sources are versioned (src/<name>/), their builds are not — dist/<name>/ is the
# synced copy that ships.
node_modules/
package-lock.json
/src/*/dist/

.DS_Store

# Rebuilt on every deploy by .github/preview.mjs
dist/_previews/
dist/index.html
.vercel
.env*
